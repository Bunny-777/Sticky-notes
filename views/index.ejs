<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="forms p-10 text-white" >
            <form action="/create" method="post">
                <input class="block  w-full rounded-md outline-none px-3 py-2 bg-zinc-800" type="text" placeholder="Title" name="title">
                <textarea  class="block  w-full rounded-md outline-none mt-2 px-3 py-2 bg-zinc-800 resize-none "placeholder="Enter the details" name="details"></textarea>
                <input  class='mt-2 bg-blue-600 px-5 py-2 rounded-md' type="submit" placeholder="Create task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task bg-zinc-800 px-3 py-3 w-72 rounded-md">
                        <h1 class="text-white text-3xl tracking-tighter" ><%= val %> </h1>
                        <a class="text-blue-500 inline-block mt-3" href="/files/<%=val %>" >Read more</a> 
                         
                        <form class="inline-block px-10 text-red-500"  action="/delete/<%= val%>" method="POST">
                            <button type="submit">Delete</button>
                          </form>
                          
                        
                    </div>
                    <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-600">No taks yet.</h3>
            <%  }  %>
        </div>
    </div>
</body>
<script src="/javascripts/script.js"></script>
</html>